<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myEducation.inside.dao.business.SchoolDao">
    
    <sql id="table">school</sql>
    <sql id="Columns">
    	id,name,headmaster,synopsis,address,entrytime
    </sql>
	
	<select id="getByName" resultType="School">
		select * from <include refid="table"/>
		where name = #{name}
	</select>
	
	<select id="getSynById" resultType="String">
		select synopsis from <include refid="table"/>
		where id = #{id}
	</select>
	
	<select id="selectByPage" resultType="School">
		select * from <include refid="table"/>
		<where>
			<if test="params.name != null and params.name != ''">
				name like CONCAT(CONCAT('%', #{params.name}), '%')
			</if>
		</where>
		order by entrytime desc
	</select>
	
	<insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="School">
		insert into <include refid="table"/>(
			name,headmaster,synopsis,address
		) values (
			#{name},#{headmaster},#{synopsis},#{address}
		)
	</insert>
	
	<update id="update">
		update <include refid="table"/> 
		set 
			name=#{name},
			headmaster=#{headmaster},
			synopsis=#{synopsis},
			address=#{address}
		where id = #{id}
	</update>
	
	<delete id="delete">
		delete from <include refid="table"/>
		where id=#{id}
	</delete>
	
	<select id="getAll" resultType="HashMap"> 
		select id as value,name as label
		from <include refid="table"></include>
		order by id
	</select>
</mapper>