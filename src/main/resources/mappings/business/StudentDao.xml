<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myEducation.inside.dao.StudentDao">
    
    <sql id="table">student</sql>
    <sql id="Columns">
    	id,name,sex,age,province,nation,s_id,g_id,c_id,entrytime,updatetime
    </sql>
	
	<select id="selectByName" resultType="Student">
		select * from <include refid="table"/>
		where name = #{name}
	</select>
	
	<select id="getById" resultType="Student">
		select * from <include refid="table"/>
		where id=#{id}
	</select>
	
	<select id="selectByPage" resultType="Student">
		select Student.*,s.name as sName,g.name as gName,c.name as cName
		from <include refid="table"/>
		left join school s on student.s_id = s.id
		left join S_grade g on student.g_id = g.id
		left join S_class c on student.c_id = c.id
		<where>
			<if test="params.name != null and params.name != ''">
				student.name like CONCAT(CONCAT('%', #{params.name}), '%')
			</if>
		</where>
		order by updatetime desc
	</select>
	
	<insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="Student">
		insert into <include refid="table"/>(
			name,sex,age,province,nation,s_id,g_id,c_id
		) values (
			#{name},#{sex},#{age},#{province},#{nation},#{s_id},#{g_id},#{c_id}
		)
	</insert>
	
	<update id="update">
		update <include refid="table"/> 
		set 
			name=#{name},
			sex=#{sex},
			age=#{age},
			province=#{province},
			nation=#{nation},
			s_id=#{s_id},
			g_id=#{g_id},
			c_id=#{c_id}
		where id = #{id}
	</update>
	
	<delete id="delete">
		delete from <include refid="table"/>
		where id=#{id}
	</delete>
</mapper>