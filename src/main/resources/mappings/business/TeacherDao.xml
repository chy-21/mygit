<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myEducation.inside.dao.business.TeacherDao">
    
    <sql id="table">teacher</sql>
    <sql id="Columns">
    	id,name,sex,age,synopsis,s_id,s_grade,s_class,entrytime,updatetime
    </sql>
	
	<select id="selectByName" resultType="Teacher">
		select * from <include refid="table"/>
		where name = #{name}
	</select>
	
	<select id="selectByPage" resultType="Teacher">
		select Teacher.*,s.name as sName
		from <include refid="table"/>
		join school s on teacher.s_id = s.id
		<where>
			<if test="params.name != null and params.name != ''">
				teacher.name like CONCAT(CONCAT('%', #{params.name}), '%')
			</if>
		</where>
		order by updatetime desc
	</select>
	
	<insert id="insert" useGeneratedKeys="true" keyProperty="id" parameterType="Teacher">
		insert into <include refid="table"/>(
			name,sex,age,synopsis,s_id,s_grade,s_class
		) values (
			#{name},#{sex},#{age},#{synopsis},#{s_id},#{s_grade},#{s_class}
		)
	</insert>
	
	<update id="update">
		update <include refid="table"/> 
		set 
			name=#{name},
			sex=#{sex},
			age=#{age},
			synopsis=#{synopsis},
			s_id=#{s_id},
			s_grade=#{s_grade},
			s_class=#{s_class}
		where id = #{id}
	</update>
	
	<delete id="delete">
		delete from <include refid="table"/>
		where id=#{id}
	</delete>
	
	<select id="getAll" resultType="HashMap">
		select id as value,name as label
		from <include refid="table"></include>
		order by id
	</select>
</mapper>